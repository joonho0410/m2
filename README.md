# 파이썬 기초 문법 퀴즈 게임

터미널에서 동작하는 4지선다 퀴즈 게임입니다. Python 기본 문법(클래스, 파일 입출력, 예외 처리)과 Git 기초(커밋, 브랜치, 원격 저장소)를 익히기 위한 미션 과제로 제작했습니다.

## 프로젝트 개요

- 실행하면 메뉴에서 번호를 선택해 퀴즈를 풀거나, 새 퀴즈를 추가하거나, 등록된 퀴즈 목록과 최고 점수를 확인할 수 있습니다.
- 추가한 퀴즈와 최고 점수는 `state.json` 파일에 저장되어, 프로그램을 종료했다가 다시 실행해도 그대로 유지됩니다.
- 숫자가 아닌 입력, 빈 입력, 범위를 벗어난 입력, `Ctrl+C`/입력 스트림 종료(EOF) 등 다양한 예외 상황을 처리하도록 구현했습니다.

## 퀴즈 주제와 선정 이유

주제는 **파이썬 기초 문법**입니다. 이 미션 자체가 파이썬 기초 문법(변수/자료형, 조건문, 반복문, 함수, 클래스)을 배우면서 진행하는 과제이기 때문에, 배운 내용을 그대로 퀴즈 문제로 만들면 학습 내용을 복습하는 데 가장 도움이 될 것이라 판단하여 이 주제를 선택했습니다.

## 실행 방법

Python 3.8 이상이 설치되어 있으면 별도의 외부 라이브러리 설치 없이 바로 실행할 수 있습니다.

```bash
git clone https://github.com/joonho0410/m2.git
cd m2
python3 main.py
```

실행 후 화면에 표시되는 메뉴에서 번호(1~5)를 입력해 원하는 기능을 선택합니다.

## 기능 목록

1. **퀴즈 풀기** – 저장된 퀴즈를 순서대로 출제하고, 정답 여부를 즉시 알려준 뒤 마지막에 총 결과를 보여줍니다. 결과가 기존 최고 점수보다 높으면 최고 점수를 갱신합니다. 등록된 퀴즈가 없으면 안내 메시지를 출력합니다.
2. **퀴즈 추가** – 문제, 선택지 4개, 정답 번호(1~4)를 입력받아 새 퀴즈를 등록하고 즉시 파일에 저장합니다.
3. **퀴즈 목록** – 현재 등록된 모든 퀴즈의 문제와 정답 번호를 출력합니다. 퀴즈가 없으면 안내 메시지를 출력합니다.
4. **점수 확인** – 저장된 최고 점수를 보여줍니다. 아직 한 번도 퀴즈를 풀지 않았다면 안내 메시지를 출력합니다.
5. **종료** – 현재 상태를 저장한 뒤 프로그램을 안전하게 종료합니다.

공통 입력 처리: 숫자를 입력해야 하는 모든 곳에서 앞뒤 공백 제거, 숫자 변환 실패, 허용 범위 밖 숫자, 빈 입력을 검사하여 안내 메시지를 출력하고 재입력을 받습니다. 또한 `Ctrl+C`(KeyboardInterrupt)나 입력 스트림 종료(EOFError)가 발생해도 프로그램이 비정상 종료되지 않고, 가능한 범위에서 데이터를 저장한 뒤 안전하게 종료합니다.

## 파일 구조

```
misson-2/
├── main.py         # 프로그램 진입점 (QuizGame 실행)
├── quiz.py          # Quiz 클래스: 개별 퀴즈(문제/선택지/정답) 표현
├── quiz_game.py      # QuizGame 클래스: 메뉴, 게임 진행, 파일 저장/불러오기
├── quiz_data.py       # 기본 퀴즈 데이터(DEFAULT_QUIZZES)
├── state.json         # 퀴즈 목록과 최고 점수를 저장하는 데이터 파일 (실행 시 자동 생성)
├── .gitignore
└── README.md
```

## 데이터 파일 설명 (state.json)

- 위치: 프로젝트 루트의 `state.json` (`main.py`, `quiz_game.py`와 같은 디렉터리)
- 인코딩: UTF-8
- 역할: 등록된 퀴즈 목록과 최고 점수를 저장하여, 프로그램을 재시작해도 데이터가 유지되도록 합니다.
- 스키마 예시:

```json
{
  "quizzes": [
    {
      "question": "다음 중 파이썬의 정수형 자료형을 나타내는 것은?",
      "choices": ["str", "int", "bool", "list"],
      "answer": 2
    }
  ],
  "best_score": 7
}
```

  - `quizzes`: 퀴즈 목록. 각 항목은 문제(`question`), 4개의 선택지(`choices`), 정답 번호(`answer`, 1~4)를 가집니다.
  - `best_score`: 지금까지 퀴즈를 푼 결과 중 가장 높은 정답 개수입니다. 아직 한 번도 풀지 않았다면 `null`입니다.

- 파일이 없으면(최초 실행) 기본 퀴즈 데이터(`quiz_data.py`)로 시작하며, 해당 데이터로 `state.json`을 새로 생성합니다.
- 파일 내용이 손상되어 읽을 수 없는 경우(JSON 파싱 실패, 필수 키 누락 등) 안내 메시지를 출력하고 기본 퀴즈 데이터로 복구합니다.

### 여러 프로그램을 동시에 실행했을 때의 데이터 동시성 처리

이 프로그램은 하나의 `state.json` 파일을 여러 프로세스가 함께 사용할 수 있다는 점을 고려해, **저장 직전에 파일을 다시 읽어 최신 내용과 병합한 뒤 저장**합니다(`QuizGame.save_data()`).

- 만약 저장할 때마다 그 시점의 메모리 값(`self.quizzes`, `self.best_score`)을 파일에 그대로 덮어썼다면, 두 프로그램을 동시에 켜고 각각 퀴즈를 추가했을 때 나중에 저장하는 프로그램이 먼저 저장된 내용을 통째로 지워버리는 문제(lost update)가 생깁니다.
- 이를 막기 위해 퀴즈를 추가하거나(`add_quiz`) 점수를 갱신할 때(`play_quiz`) 다음 순서로 동작합니다.
  1. 저장하기 직전, `state.json`을 다시 읽어 그 사이 다른 프로그램이 저장해 둔 최신 퀴즈 목록/최고 점수를 가져옵니다.
  2. 그 최신 데이터 위에 이번에 추가한 퀴즈, 또는 이번에 달성한 점수(기존 최고 점수보다 높을 때만)만 얹습니다.
  3. 합쳐진 결과를 파일에 저장합니다.
- 이 방식(다시 읽고 병합하기)은 파일 잠금(file lock)을 쓰는 방법보다 구현이 간단하지만, "다시 읽는 시점"과 "쓰는 시점" 사이에 아주 짧은 틈이 남아 있어 완벽한 보장은 아닙니다. 다만 이 프로젝트처럼 동시 접속이 드문 개인용 CLI 프로그램에서는 이 정도로 충분하며, 더 강한 보장이 필요하면 `fcntl.flock` 같은 파일 잠금을 추가로 도입할 수 있습니다.

> 이 줄은 clone/pull 실습을 위해 별도 디렉터리(misson-2-clone)에서 추가되었습니다.

## 제출물 (텍스트로 대체한 스크린샷)

### GitHub 저장소 URL

https://github.com/joonho0410/m2

### 개발 환경

```
$ python3 --version
Python 3.14.5

$ git --version
git version 2.40.0

$ git log -1 --format='%an <%ae>'
Junho jeon <junhjeon@Junhoui-MacBookAir-3.local>
```

### 프로그램 실행 결과 (퀴즈 추가 / 목록 / 플레이 / 점수)

```
퀴즈 게임을 시작합니다.

==============================
       파이썬 기초 퀴즈 게임
==============================
1. 퀴즈 풀기
2. 퀴즈 추가
3. 퀴즈 목록
4. 점수 확인
5. 종료
메뉴를 선택하세요: 3

[퀴즈 목록]
1. 다음 중 파이썬의 정수형 자료형을 나타내는 것은? (정답: 2번)
...(생략)...
7. __init__ 메서드에 대한 설명으로 옳은 것은? (정답: 2번)

메뉴를 선택하세요: 2

[퀴즈 추가]
문제를 입력하세요: 파이썬에서 리스트를 정렬하는 내장 함수는?
선택지 1을(를) 입력하세요: sort()
선택지 2을(를) 입력하세요: len()
선택지 3을(를) 입력하세요: append()
선택지 4을(를) 입력하세요: pop()
정답 번호(1~4)를 입력하세요: 1
퀴즈가 추가되었습니다.

메뉴를 선택하세요: 1

총 8문제를 출제합니다.

[1번 문제] 다음 중 파이썬의 정수형 자료형을 나타내는 것은?
  1. str
  2. int
  3. bool
  4. list
정답 번호를 입력하세요: 2
정답입니다!
...(생략, 8문제 순차 진행)...

결과: 총 8문제 중 3문제를 맞혔습니다.
최고 점수를 갱신했습니다!

메뉴를 선택하세요: 4

[점수 확인]
최고 점수: 3

메뉴를 선택하세요: 5

게임을 종료합니다. 이용해 주셔서 감사합니다.
```

**예외 입력 처리 데모** (문자 입력 `abc`, 빈 입력, 범위 밖 숫자 `9`):

```
메뉴를 선택하세요: abc
숫자만 입력해 주세요.
메뉴를 선택하세요: 
입력이 비어 있습니다. 다시 입력해 주세요.
메뉴를 선택하세요: 9
1~5 사이의 숫자를 입력해 주세요.
메뉴를 선택하세요: 1
```

**재실행 시 데이터 유지 확인** (프로그램을 종료 후 다시 실행):

```
$ python3 main.py 로 재실행
메뉴를 선택하세요: 3   -> 8개 퀴즈(기본 7개 + 추가한 1개) 그대로 출력됨
메뉴를 선택하세요: 4   -> 최고 점수: 3 그대로 유지됨
```

### `git log --oneline --graph` 결과

```
* ccf1d2d 동시 저장 시 데이터 유실 방지: 저장 직전 재읽기 후 병합하는 방식으로 save_data 변경, README에 설명 추가
* 8996abe README에 clone/pull 실습용 한 줄 추가
* f8f160b README 작성: 프로젝트 개요, 주제 선정 이유, 실행 방법, 기능 목록, 파일 구조, state.json 설명
* 9b205bb state.json 저장/불러오기 구현: UTF-8 저장, 파일 없음/손상 시 기본 데이터로 복구
* 0712b4e QuizGame 클래스 구조 정리: 기본 퀴즈 데이터를 quiz_data.py로 분리, 메뉴 dispatch를 딕셔너리로 정리
* e9c352c 점수 확인 기능 구현: 최고 점수 조회 및 퀴즈 풀이 시 갱신, 미실행 상태 처리
* 1d2d1cd 퀴즈 목록 기능 구현: 등록된 퀴즈 전체 출력, 빈 목록 처리
* aa727c5 퀴즈 추가 기능 구현: 문제/선택지 4개/정답 입력 및 등록, 입력 검증 적용
*   a40796f Merge branch 'feature/play-quiz' into main
|\  
| * 4922a2b 퀴즈 풀기 기능 구현: 순차 출제, 정답 확인, 결과 표시, 빈 퀴즈 처리
|/  
* 3065667 기본 퀴즈 데이터 작성: 파이썬 기초 문법 주제 7문제 추가
* 0e63880 Quiz 클래스 작성: 문제/선택지/정답 속성 및 출력, 정답 확인, 직렬화 메서드
* e8ef258 메뉴 기능 구현: 메뉴 출력/선택 및 공통 입력 검증(공백/숫자변환/범위/빈입력/Ctrl+C,EOF 처리)
* 1585da7 Git 저장소 초기 설정: .gitignore 추가
* cad187a Initial commit
```

## 추가로 확인·구현하면 좋은 것

미션 요구사항을 기준으로 검토한 결과, 핵심 기능(메뉴/Quiz·QuizGame 클래스/4개 기능/state.json 영속성/공통 입력 예외 처리/브랜치 생성·병합)은 모두 구현되어 있고 커밋도 14개로 요구 개수(10개)를 충족합니다. 다만 아래 항목은 점검 또는 보완이 필요합니다.

1. **`git init` 사용 이력 확인 필요**
   - 이 로컬 저장소의 reflog상 최초 기록이 `git init`이 아니라 `clone: from https://github.com/joonho0410/m2.git`으로 시작합니다. GitHub에서 저장소를 먼저 만들고 로컬로 clone하는 방식으로 진행된 것으로 보이며, 이 경우 로컬에서 `git init`을 직접 실행한 이력이 없습니다.
   - 제약 사항의 "Git 기초 명령어 7종(init, add, commit, push, pull, checkout, clone)을 각각 한 번 이상 사용"을 문자 그대로 만족하려면, 별도의 로컬 디렉터리에서 `git init` 후 `git remote add origin ...`으로 원격을 연결해보는 실습을 한 번 추가하는 것을 권장합니다. (이미 다른 저장소나 실습 과정에서 `git init`을 사용한 적이 있다면 문제 없습니다.)

2. **보너스 과제는 아직 미구현**
   - 랜덤 출제(`random.shuffle` 등으로 문제 순서 섞기)
   - 풀 문제 수 선택 기능
   - `Quiz`에 힌트 속성 추가 및 힌트 사용 시 점수 차감
   - 등록된 퀴즈 삭제 기능 (파일 반영 포함)
   - 점수 기록 히스토리 (날짜/시간, 푼 문제 수, 점수를 리스트로 누적 저장)
   - 필수 요구사항은 아니므로 시간이 남을 때 선택적으로 구현하면 됩니다.

3. **`state.json`이 `.gitignore`에 포함되어 있음**
   - 사용자별 로컬 데이터(추가한 퀴즈, 최고 점수)이므로 커밋하지 않는 것이 합리적인 선택이지만, 과제 채점자가 "실제 저장되는 데이터 형태"를 저장소에서 바로 확인하고 싶을 수 있습니다. 필요하다면 `state.example.json` 같은 샘플 파일을 하나 커밋해 두는 것도 고려할 수 있습니다. (README에는 이미 스키마 예시가 포함되어 있어 필수는 아닙니다.)

4. **제출물 캡처**
   - 위 "제출물" 섹션에 텍스트로 실행 결과·환경 정보·`git log` 결과를 정리해 두었습니다. 실제 제출 시 이미지 스크린샷이 필요하다면 이 텍스트를 참고해 동일한 화면을 캡처하면 됩니다.
